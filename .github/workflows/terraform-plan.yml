name: Terraform Plan & Validate

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - "infra/**"
      - "envs/**"
      - ".github/workflows/terraform-plan.yml"

concurrency:
  group: terraform-plan-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write   # Request OIDC JWT
  contents: read
  pull-requests: write

jobs:
  plan-dev:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-plan.yml@v1.0.0
    with:
      environment: "dev"
      chdir: "infra/stack"
    secrets: inherit

  plan-staging:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-plan.yml@v1.0.0
    with:
      environment: "staging"
      chdir: "infra/stack"
    secrets: inherit

  plan-prod:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-plan.yml@v1.0.0
    with:
      environment: "prod"
      chdir: "infra/stack"
    secrets: inherit
