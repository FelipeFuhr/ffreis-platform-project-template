name: DevOps / Terraform Checks

on:
  pull_request:
    paths:
      - "**/*.tf"
      - ".github/workflows/**"
  push:
    branches: [main, master]
    paths:
      - "**/*.tf"
      - ".github/workflows/**"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fmt:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-fmt.yml@v1.0.0
    with:
      working-directory: "."

  validate:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-validate.yml@v1.0.0
    needs: [fmt]
    with:
      chdir: "infra/stack"
      validate-all-modules: true
      modules-dir: "infra/modules"

  lint:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-lint.yml@v1.0.0
    needs: [fmt]
    with:
      lint-path: "."

  trivy-config:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-security.yml@v1.0.0
    needs: [fmt]
    with:
      scan-ref: "."

  tfsec:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-tfsec.yml@v1.0.0
    needs: [fmt]
    with:
      scan-path: "."
      minimum-severity: "HIGH"
      soft-fail: false

  checkov:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-checkov.yml@v1.0.0
    needs: [fmt]
    with:
      scan-directory: "."
      framework: "terraform"
      soft-fail: false

  semgrep:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-semgrep.yml@v1.0.0
    needs: [fmt]
    with:
      working-directory: "."
      config: "p/terraform"

