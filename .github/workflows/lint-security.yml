name: CI (static checks)

# Runs on every PR touching Terraform or workflow files.
# Job dependency graph:
#
#   secret-scan ─┐
#   fmt ─────────┼─→ validate
#                ├─→ lint
#                ├─→ security  (trivy)
#                └─→ tfsec
#
# All jobs fail hard — no continue-on-error.

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "infra/**"
      - "envs/**"
      - ".tflint.hcl"
      - ".github/workflows/lint-security.yml"

concurrency:
  group: ci-static-${{ github.ref }}
  cancel-in-progress: true

jobs:

  # ── 1. Secret scan ─────────────────────────────────────────────────────────
  secret-scan:
    name: Secret Scan
    permissions:
      contents: read
      actions: read
      security-events: write
    uses: FelipeFuhr/ffreis-workflows-general/.github/workflows/general-gitleaks.yml@82097e621b8f630f2be2f759025529f7f5442796 # v1.9.0
    with:
      working-directory: "."
      scan-history: true
      upload-sarif: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false }}

  fmt:
    permissions:
      contents: read
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-fmt.yml@5606584d4d99f3f0886517c4e637aa0b19308898 # v1.1.3
    with:
      working-directory: "."

  # ── 3. Validate ─────────────────────────────────────────────────────────────
  validate:
    needs: [fmt]
    permissions:
      contents: read
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-validate.yml@5606584d4d99f3f0886517c4e637aa0b19308898 # v1.1.3
    with:
      chdir: "infra/stack"

  # ── 4. Lint ─────────────────────────────────────────────────────────────────
  lint:
    needs: [fmt]
    permissions:
      contents: read
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-lint.yml@5606584d4d99f3f0886517c4e637aa0b19308898 # v1.1.3
    with:
      lint-path: "infra/"

  # ── 5. Security (Trivy) ──────────────────────────────────────────────────────
  security:
    needs: [fmt]
    permissions:
      contents: read
      # scan-fix(startup_failure): callee tf-security.yml also requires
      # actions: read.
      actions: read
      security-events: write
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-security.yml@5606584d4d99f3f0886517c4e637aa0b19308898 # v1.1.3
    with:
      scan-ref: "infra/"

  # ── 6. Security (tfsec) ──────────────────────────────────────────────────────
  tfsec:
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.draft == false }}
    needs: [fmt]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450 # v2.19.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: tfsec
        uses: aquasecurity/tfsec-action@b466648d6e39e7c75324f25d83891162a721f2d6 # v1.0.4
        with:
          working_directory: "infra/"
          additional_args: "--minimum-severity HIGH"
          github_token: ${{ github.token }}
